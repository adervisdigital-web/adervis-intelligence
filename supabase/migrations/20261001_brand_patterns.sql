-- Брендбук: паттерны из знака.
--
-- Собираются в коде из контура знака, а не рисуются заново. Логика взята
-- из исходников студии: паттерн тон в тон, почти незаметный — он фон,
-- а не главный герой макета.

insert into public.brand (id, title, kind, sort, section, data) values
('mark-patterns', 'Паттерн из знака', 'figure', 87, 'Материалы', jsonb_build_object(
  'figure', 'marks',
  'body', E'Четыре готовых варианта. Каждый скачивается отдельным файлом SVG и тянется на любой размер без потери качества.\n\n— Паттерн — фон: поверх него должен читаться текст\n— Тон в тон: контраст к фону держим низким\n— Золотой вариант — только для крупных плашек, не под текст\n— Знак в паттерне не перерисовываем и не растягиваем'))
on conflict (id) do nothing;
